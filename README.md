# README sections to replace

Three blocks below replace the corresponding blocks in the current README.
Everything else in the repo README (structure, profiles, quick start, D4,
reproducibility, tests, license) is accurate against the camera-ready paper
and needs no change.

---

## 1. Replace the opening quote block

**Current (from the v3 outline — stronger than the accepted paper):**

> **Mechanism claim.** Multi-turn adversarial erosion is the primary failure
> mode of Socratic LLM tutors. Per-turn constraint re-injection is the only
> mechanism we identify that resists it — independent of model, prompt content,
> or framework.

**Replace with:**

```markdown
Reference implementation of *"SocraGuard: Benchmarking Adversarial Robustness
of Socratic LLM Tutors and a Skill-based Defense"*, ICONIP 2026.

> **Mechanism claim.** Multi-turn adversarial erosion is a main failure mode of
> Socratic LLM tutors, and per-turn constraint re-injection is the strongest
> mechanism among those we test, with effects that persist across models, prompt
> wordings, and implementation frameworks. The claim is isolated by a
> same-content contrast: D2 and D3 use identical SKILL content and differ only in
> injection schedule (D3 reduces average ASR from 27.9% to 13.0% across four
> backends).
```

---

## 2. Replace the "Mapping to the paper" table

The current table uses numbering from an earlier draft. The camera-ready paper
has 7 tables, 3 figures, and no appendices. Corrected mapping:

```markdown
## Mapping to the paper

| Paper artifact                        | Generated by                                 |
| ------------------------------------- | -------------------------------------------- |
| Table 1 (Attack taxonomy)             | `configs/attack_templates.yaml`              |
| Table 2 (Ten-turn erosion ladder)     | `src/attacks.py`                             |
| Table 4 (Main results, ASR)           | `04_analyze.py` → `table_main.csv`           |
| Table 5 (Stage-wise leakage rate)     | `04_analyze.py` → `table_stagewise.csv`      |
| Table 6 (Ablation study)              | `04_analyze.py` → `summary.json`             |
| Table 7 (Human evaluation)            | `src/stats.py::cohens_kappa`                 |
| Figure 3 (Kaplan–Meier survival)      | `04_analyze.py` → `figure_km_curve.pdf`      |
| SKILL specification (§5.3)            | `data/socraguard_skill.md`                   |

Not in the paper: `figure_heatmap.pdf` and `figure_cross_backend.pdf` are
produced by `04_analyze.py` but were cut from the 15-page camera-ready version.
The per-attack-class breakdown behind §6.3 and the cross-backend numbers in §6.4
come from these outputs.
```

---

## 3. Replace the citation block

```markdown
## Citation

```bibtex
@inproceedings{zhang2026socraguard,
  title     = {SocraGuard: Benchmarking Adversarial Robustness of Socratic
               LLM Tutors and a Skill-based Defense},
  author    = {Zhang, Xixiang and Li, Jiayin and Zhang, Zhi},
  booktitle = {Proceedings of the 33rd International Conference on Neural
               Information Processing (ICONIP)},
  year      = {2026},
  publisher = {Springer},
  note      = {To appear}
}
```
```

Once the proceedings are online, replace `note = {To appear}` with the
`series`, `volume`, `pages`, and `doi` fields from the Springer page.

---

## Note on the two extra figures

Keeping `figure_heatmap.pdf` and `figure_cross_backend.pdf` in the repo is a
plus, not a problem — they are analyses the paper describes in prose (§6.3
per-attack-class breakdown, §6.4 cross-backend) but had no room to plot. The
mapping table above says so explicitly, so a reader is not left wondering why
the repo produces figures the paper does not contain.
